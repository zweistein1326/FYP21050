import React from 'react';

const RegisterScreen = () => {
    return (
        <div>
            <form onSubmit={() => { }} >
                <input name=""/>
            </form>
        </div>
    );
}

export default RegisterScreen